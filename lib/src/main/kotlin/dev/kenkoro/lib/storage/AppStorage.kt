package dev.kenkoro.lib.storage

import javax.inject.Inject

internal class AppStorage @Inject constructor() : BaseStorage {
    // TODO: Customize as you wish, e.g. you can add the following:
    // private val sampleDb = SampleDatabase(sqlDriver)
}
